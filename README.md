# wiscobash

check for application binary first and then if not found install

is there a logical way to use `/etc/bash.bashrc` (which is global) to help with this? so we don't have to mess with `~/.bashrc` at all?