# version

get an specific version info on critical applications:

```shell
bash --version && \
starship --version && \
python --version && \
ansible --version && \
terraform --version && \
tofu --version && \
virt-manager --version && \
niri --version
```

>need to update to bracket results and add to `~/.wiscobach.sh` environment